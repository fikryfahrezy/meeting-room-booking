/**
 * MSW base URL — must match NUXT_PUBLIC_API_BASE (default: http://localhost:8080).
 * All handlers use this so changing the API base only requires one edit here.
 */
export const API_BASE = "http://localhost:8080";
